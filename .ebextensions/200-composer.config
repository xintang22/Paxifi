commands:
  200-composer-update:
    command: "export COMPOSER_HOME=/root && composer.phar self-update -n"

container_commands:
  210-run-composer:
    command: "export COMPOSER_HOME=/root && composer.phar update --no-dev"
